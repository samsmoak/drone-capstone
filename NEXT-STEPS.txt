================================================================================
CROPWATCHER — WHAT IS LEFT TO BUILD
Handoff note. Written 2026-09-16.
================================================================================

Read CLAUDE.md first — it has the architecture, the flight invariants, and the
deployment traps. This file is only the to-do list.

Live site:  https://drone-capstone.vercel.app
Repo:       https://github.com/samsmoak/drone-capstone  (private)
Supabase:   fuwojlqpavvfgmqardfo


--------------------------------------------------------------------------------
HONEST STATUS
--------------------------------------------------------------------------------

DONE and verified:
  - Python flight agent: flight core, preflight gates, thermal correction,
    geofence + occupancy, missions, CSV/Supabase sinks, REST + WebSocket API,
    manual control loop.  177 tests passing, ruff + mypy clean.
  - Supabase: 7 tables, RLS on all of them, atomic mission claiming, applied
    and verified on the HOSTED project.
  - All 7 operator pages (Task 1), driven in a real browser against local
    Supabase, light + dark, phone width. Gates: typecheck, lint, build pass.
  - Tauri desktop app: macOS arm64 .dmg built locally; the PyInstaller agent
    runs from inside the bundle, loads libusb, serves HTTP + WebSocket, and
    exits with the app (including on kill -9).
  - Deployment: Vercel builds from web/ and auto-deploys on push to main.

NOT done:
  - Task 2 public pages (/project, /project/[slug], /gallery, /team, /admin).
  - Task 3 partly: the CI workflow has never run, no Windows build exists,
    and migration 0005 (installers bucket) is NOT on the hosted project yet.
  - Task 4 ML.
  - Nothing has ever talked to a real Crazyradio (no dongle yet).

Docs: docs/README.md routes to feature docs for everything above. Four
platform docs it used to link never existed; the router now says so.

--------------------------------------------------------------------------------
BEFORE YOU START — THREE THINGS ONLY THE OWNER CAN DO
--------------------------------------------------------------------------------

1. ROTATE THE SUPABASE SECRET KEY.
   sb_secret_-uaeQv8... was pasted into a chat transcript. It bypasses every
   RLS policy. Dashboard -> Settings -> API Keys.
   Nothing in this repo uses it, so rotating breaks nothing.
   Rotate the database password too, for the same reason.

2. GOOGLE SIGN-IN NEEDS A REDIRECT URL.
   Supabase -> Authentication -> URL Configuration -> Redirect URLs, add:
     https://drone-capstone.vercel.app/auth/callback
   Email/password already works without this.

3. TWO JUNK VERCEL PROJECTS EXIST: "agent" and "agent-kpqe".
   They are still connected to the repo and FAIL ON EVERY PUSH to main (Error:
   "No FastAPI entrypoint found") — those are the failed deployment emails.
   drone-capstone itself deploys fine. Delete both projects in the Vercel
   dashboard to stop the failures.


--------------------------------------------------------------------------------
TASK 1 — OPERATOR PAGES  (DONE)
--------------------------------------------------------------------------------

  /app/live, /app/flights, /app/flights/[id], /app/compare, /app/zones,
  /app/plan, /app/manual, /app/settings — all built.
  See docs/features/frontend/operator-dashboard.txt and mission-planner.txt.

  Known limits:
  - Zone health colours assume health_score is 0–1. Pin it when Task 4 lands.
  - Planner leg timing assumes 0.5 m/s cruise; nothing measured that.
  - Browser manual control needs the browser to allow https -> ws://127.0.0.1.
    Not tried from the live site yet. The desktop app does not have this issue.

--------------------------------------------------------------------------------
TASK 2 — PUBLIC PAGES
--------------------------------------------------------------------------------

  /project             The capstone report. Copy-adapt doctor-portfolio's
                       BlockNote setup (lib/blocknote/, ServerBlockNoteEditor).
  /project/[slug]      Deep-dives: hardware, flight control, correction, ML.
  /gallery             Kit photos are already in web/public/hardware/.
  /team                The five members.
  /admin               BlockNote CMS for the above. Gate on role='operator'.

Already built: /, /setup, /setup/hardware, /login, /auth/callback, /app.


--------------------------------------------------------------------------------
TASK 3 — TAURI DESKTOP APP  (mostly done)
--------------------------------------------------------------------------------

  Done: scaffold, three panels, PyInstaller sidecar, externalBin, release
  workflow, installers bucket migration, /setup download buttons.
  See docs/features/desktop/desktop-app.txt and pages-and-windows.txt.

  Left:
  a. Push a v* tag and watch .github/workflows/desktop-release.yml run. It has
     never run. Add repo secrets SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY for
     the upload step (it skips cleanly without them).
  b. Apply migration 0005 to the hosted project: cd backend && supabase db push.
  c. PROPOSED, NOT STARTED — vitals in the desktop app for manual AND
     autonomous flights. Two gaps found while checking whether it can be done:
       - `serve` does not run the mission poller, so the installed desktop app
         would never fly missions queued on the website.
       - manual control starts no TelemetryReader, so manual flights record
         nothing at all.
     Plan: run the poller inside the desktop agent; add an in-memory broadcast
     sink + local /ws/telemetry; start the reader during manual control too.
     Needs a lab check: logging and 50 Hz control share the radio.

  Build locally: backend/agent/packaging/build_sidecar.sh, then
  cd desktop && pnpm tauri build.  Rust is in ~/.cargo/bin (not always on PATH).
  PyInstaller is installed in backend/agent/.venv.

--------------------------------------------------------------------------------
TASK 4 — ML
--------------------------------------------------------------------------------

  - Port feature extraction from the old notebooks into
    backend/agent/cropwatcher/telemetry/features.py
  - Load best_lnn_stable.pt (in the OLD repo, ~/Desktop/drone project/code/),
    run inference after correction, write rows to the predictions table.
  - Inference runs in the agent, never in the browser. The model is a .pt file.


--------------------------------------------------------------------------------
NEVER VERIFIED ON HARDWARE — DO THIS IN THE LAB
--------------------------------------------------------------------------------

Everything drone-facing has only ever run against a fake. With the Crazyradio
plugged in and a charged battery:

    cd backend/agent && source .venv/bin/activate
    cropwatcher check
    cropwatcher hover --height 0.3 --secs 30 --ambient 74F

The second should reproduce the 25-second held hover from bring-up and write a
CSV into organized_flights/.

Then run the hardware test suite:

    pytest -m hardware


--------------------------------------------------------------------------------
GATES — RUN BEFORE EVERY COMMIT
--------------------------------------------------------------------------------

    cd backend/agent && source .venv/bin/activate
    ruff check . && mypy cropwatcher && pytest

    cd web
    pnpm typecheck && pnpm lint && pnpm build
    # typecheck runs `next typegen` first: bare tsc fails on a fresh clone,
    # because PageProps route types only exist after generation.

    cd desktop
    pnpm exec tsc --noEmit

Report pass/fail/skipped honestly. Do not claim done on an unrun gate.


--------------------------------------------------------------------------------
TRAPS THAT ALREADY COST TIME
--------------------------------------------------------------------------------

- Vercel Root Directory MUST stay "web". Unset, Vercel finds
  backend/agent/pyproject.toml, detects FastAPI, and tries to deploy the flight
  agent. Never give it a FastAPI entrypoint — the agent needs a USB radio and
  cannot run in the cloud.

- Hosted Supabase grants authenticated ALL privileges on public by default;
  local does not. Migration 0004 revokes and re-grants explicitly. If you add a
  table, check its grants on the HOSTED project, not just locally.

- A security test that only checks for denial proves nothing. Confirm the
  permitted path works too, or you cannot tell a policy from a missing grant.

- Seeding auth.users requires empty strings, not NULL, in the token columns.
  NULL makes every sign-in fail with a 500 that says "Database error querying
  schema" and looks nothing like a seeding problem.

- Do not run `supabase db reset` against the hosted project. It drops
  everything. Use `db push`.

- Cut motors is P, not Space (operator's decision). Space is blocked on keydown
  AND keyup — WebKit activates a focused button on Space keyup.

- The local Supabase database holds synthetic test data added while building
  the pages: flights aaaaaaaa-…-0001 to -0004, their telemetry, 8 predictions
  and a few missions. Local only; `supabase db reset` (LOCAL) clears it.

================================================================================
