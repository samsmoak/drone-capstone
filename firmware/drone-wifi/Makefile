# Out-of-tree Crazyflie app: stock firmware plus src/drone_wifi.c.
# Build with ./build.sh, which pins the firmware release and the compiler.
CRAZYFLIE_BASE ?= $(HOME)/.cache/cropwatcher/crazyflie-firmware
OOT_CONFIG := $(PWD)/app-config

include $(CRAZYFLIE_BASE)/tools/make/oot.mk
